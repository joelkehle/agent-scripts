#!/usr/bin/env bash
set -euo pipefail

TARGET="$HOME/Projects/manager/bin/machine-compliance"

if [ -x "$TARGET" ]; then
  exec "$TARGET" "$@"
fi

echo "machine-compliance: missing $TARGET" >&2
echo "Clone/update ~/Projects/manager and retry." >&2
exit 1
