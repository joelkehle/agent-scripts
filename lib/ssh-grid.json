{
  "comment": "Approved 6x6 SSH grid by source host. Cells not listed are blocked. Keystone is human-only from the laptop and out of scope for agents.",
  "hosts": ["laptop", "dev", "beelink", "lab", "keystone", "macmini"],
  "hostAliasesComment": "Lowercase real hostnames -> grid names. Only list hostnames that differ from the grid name.",
  "hostAliases": {
    "joelsurface5": "laptop"
  },
  "grid": {
    "laptop": {
      "dev": { "method": "alias", "command": "agent-dev" },
      "beelink": { "method": "alias", "command": "agent-beelink" },
      "lab": { "method": "alias", "command": "agent-lab" },
      "macmini": { "method": "alias", "command": "agent-macmini" }
    },
    "dev": {
      "beelink": { "method": "sudo-agent", "command": "agent-ssh beelink" },
      "lab": { "method": "sudo-agent", "command": "agent-ssh lab" },
      "macmini": { "method": "sudo-agent", "command": "agent-ssh macmini" }
    },
    "beelink": {
      "keystone": { "method": "tunnel-only", "command": null, "note": "tunnel only, no shell" }
    },
    "lab": {},
    "keystone": {},
    "macmini": {}
  },
  "rule": "Never use joelkehle keys or accounts for automation."
}
