{
  "scripts": {
    "agent:check": "bash -n bin/agent-check && bash -n bin/loop-audit && node --check bin/docs-list && bash tests/agent-loop-tools.sh"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "puppeteer-core": "^24.34.0"
  }
}
