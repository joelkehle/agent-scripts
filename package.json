{
  "scripts": {
    "agent:check": "bash -n bin/agent-check && bash -n bin/loop-audit && node --check bin/codex-bg && node --check lib/codex-bg-email.js && node --check bin/docs-list && node --check bin/dirty-audit && node --check bin/loop-receipt && node --check bin/loop-resume && bash tests/agent-loop-tools.sh"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "puppeteer-core": "^24.34.0"
  }
}
